type User {
  id: String
  username: String
}

type Mutation {
  register(username: String!, password: String!): String
  login(username: String!, password: String!): String
}

type Query {
  getCurrentUser: User
}

schema {
  mutation: Mutation,
  query: Query
}